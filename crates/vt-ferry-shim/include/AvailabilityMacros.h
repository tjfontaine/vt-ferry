// Linux-side stub for Apple's <AvailabilityMacros.h>. Empty —
// every macro it would define is a no-op annotation that doesn't
// affect codegen on Linux.
#ifndef VTF_LINUX_AVAILABILITY_MACROS_H
#define VTF_LINUX_AVAILABILITY_MACROS_H

#include <Availability.h>

#endif
